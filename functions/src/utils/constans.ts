export const PAGE_SIZE = 20; // Pagination size to limit number of requets to the API
export const API_LIMIT = 1500; // Limit of daily requets per WU API key: 1500
export const MINUTES_TO_FETCH_NEW_DATA = 15; // Gets data from DB if user reloads within this time.
